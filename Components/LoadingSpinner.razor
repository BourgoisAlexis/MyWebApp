<div class="d-flex align-content-center">
    <i class="loading-icon bi-bootstrap"></i>
</div>

<style>
    .loading-icon {
        position: absolute;
        top: 50%;
        left: 50%;
        margin-top: -1.5rem;
        margin-left: -1.5rem;
        font-size: 3rem;
        animation: loading 2s ease-in-out infinite;
    }

    @@keyframes loading {
        0% {
            transform: rotateZ(0deg) scale(0.8);
        }

        50% {
            transform: rotateZ(360deg) scale(1.2);
        }

        100% {
            transform: rotateZ(720deg) scale(0.8);
        }
    }
</style>
